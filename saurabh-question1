 What is cherry-pick? Explain the use case of it also write steps to do it.?
-> git cherry pick is the choose the commit from one branch to another branch

  command : git cherry-pick <commmit_ID>

  use case :
	If developer make a commit in other branch rather than neccessary branch then we have to need the cherry-pick command

  steps :
	
	1) let we have to branches branchA and branchB
	2) perforn the commit operation on one branch lets say branchA
	3) now we have to get the commit of branchA to branchB we use the cherry-pick <commit_ID>
